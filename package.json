{
    "name": "reader",
    "displayName": "vsc-extension",
    "description": "read webview",
    "version": "0.0.1",
    "publisher": "renkun",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.reader"
    ],
    "main": "./src/extension",
    "contributes": {
        "commands": [
            {
                "command": "extension.reader",
                "title": "reader"
            }
        ]
    },
    "scripts": {
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^3.1.4",
        "vscode": "^1.1.25",
        "eslint": "^4.11.0",
        "@types/node": "^8.10.25",
        "@types/mocha": "^2.2.42"
    },
    "dependencies": {
    }
}
